export const SettingsPage = () => (
  <section class="card p-5 sm:p-6">
    <span class="tag grey">Settings</span>
    <h2 class="mt-4 text-3xl font-bold tracking-tight">Preferences</h2>
    <p class="mt-3 max-w-2xl text-text-secondary">
      App preferences and wallet behavior will live here. Theme is global already; more settings can slot into this layout.
    </p>
  </section>
);
